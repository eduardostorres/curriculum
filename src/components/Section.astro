---
interface Props {
  title?: string;
  translationKey?: string;
}

const { title, translationKey } = Astro.props;
---

<section class="m-auto mt-2 max-w-4xl">
  {
    title && (
      <h2
        class="font-bold mt-8 text-3xl font-sans"
        data-i18n={translationKey}
      >
        {title}
      </h2>
    )
  }
  <slot />
</section>
